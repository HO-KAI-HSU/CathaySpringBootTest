CREATE TABLE CURRENCY_TYPE1 (
  ID INTEGER NOT NULL PRIMARY KEY AUTO_INCREMENT,
  CODE VARCHAR(255) NOT NULL,
  NAME VARCHAR(255) NOT NULL,
  STATUS INT DEFAULT 1,
  CREATE_TIME TIMESTAMP NOT NULL,
  UPDATE_TIME TIMESTAMP,
  PRIMARY KEY (ID)
);